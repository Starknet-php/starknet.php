<?php

beforeEach(function () {
});

it('gets a contract address', function () {
});

it('can call a contract and return a valid response', function () {
});

it('it can get the current block and information', function () {
});

it('it can get the code deployed in a contract', function () {
});

it('it can get a storage variable in a contract located at a specific key', function () {
});

it('it can get the status of a transaction', function () {
});

it('it can get transaction information', function () {
});

it('it can call function in a starknet contract', function () {
});

it('it can deploy a contract', function () {
});

